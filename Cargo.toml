[package]
name = "hyper-req-log"
version = "0.3.0"
edition = "2021"

[dependencies]
hyper = "1.0"

[dev-dependencies]
bytes = "1.6.0"
http-body-util = "0.1"
hyper-util = { version = "0.1", features = ["full"] }
tokio = { version = "1.37.0", features = ["full"] }
